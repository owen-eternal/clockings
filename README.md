#This RestAPI receives employee timestamps from an employers browser and logs it in a Postgres database.
#It receives a json object as a resource through a post method.
#response data includes: clock-in/ clock-out status data, png base64 string, geolocation, employee comment.
